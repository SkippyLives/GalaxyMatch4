// Setup the difficulty levels
playerType_AI = 0;
playerType_Human = 1;
playerType_Max = playerType_Human + 1;

// Position the object
x = ( (room_width/2) - (sprite_get_width(sprPlayerType)/2));
y = room_height - (sprite_get_height(sprPlayerType) * 2.5);

