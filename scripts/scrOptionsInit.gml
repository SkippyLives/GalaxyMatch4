options = false;
// Position the object
x = (room_width - ( (room_width - (sprite_get_width(sprGrid) + 10))/2)) - (sprite_get_width(sprOptions)/2);
y = room_height - (sprite_get_height(sprOptions) * 2.5);

